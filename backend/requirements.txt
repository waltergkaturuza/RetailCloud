Django==4.2.7
djangorestframework==3.14.0
django-cors-headers==4.3.0
django-extensions==3.2.3
psycopg2-binary==2.9.9  # Required for PostgreSQL
python-dotenv==1.0.0
gunicorn==21.2.0  # Production WSGI server
whitenoise==6.6.0  # Static file serving for production
Pillow>=10.2.0

# OCR and Image Processing for Bulk Inventory
opencv-python==4.8.1.78
pytesseract==0.3.10
pyzbar==0.1.9
django-filter==23.5
djangorestframework-simplejwt==5.3.1
celery==5.3.4
redis==5.0.1
channels==4.0.0  # Django Channels for WebSocket support
channels-redis==4.2.0  # Redis channel layer for Channels
stripe==7.8.0
python-dateutil==2.8.2
openpyxl==3.1.2
reportlab==4.0.7
weasyprint==60.2  # HTML to PDF for quotations and invoices
qrcode==7.4.2
phonenumbers==8.13.27
pyotp==2.9.0
django-ratelimit==4.1.0
# barcode - optional, install separately if needed: pip install python-barcode
# pandas and numpy - optional for analytics, install separately if needed
# pandas>=2.2.0
# numpy>=2.0.0
# SMS and SSO dependencies (optional - install as needed)
# twilio>=8.0.0  # For SMS 2FA and SMS marketing: pip install twilio
# boto3>=1.28.0  # For AWS SNS SMS: pip install boto3
# django-allauth>=0.54.0  # For OAuth2 SSO: pip install django-allauth
# python-saml>=3.0.0  # For SAML SSO: pip install python3-saml
# python-ldap>=3.4.0  # For LDAP SSO: pip install python-ldap

